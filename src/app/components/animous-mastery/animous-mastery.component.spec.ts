import { AnimousMasteryComponent } from './animous-mastery.component';

describe('AnimousMasteryComponent', () => {
  let component: AnimousMasteryComponent;

  beforeEach(() => {
    component = new AnimousMasteryComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
